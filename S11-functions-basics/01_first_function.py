# 'def' defines a function. The body is indented, exactly like a loop body.
# Defining a function does NOT run it.
def hello():
    print('Hello World!')

# The function only runs when we CALL it, with parentheses.
hello()
hello()
hello()
